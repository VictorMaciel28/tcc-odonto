{
  "name": "tcc-odonto",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "set NODE_ENV=production&& vite build --mode production",
    "preview": "vite preview",
    "deploy": "cd dist && cp index.html 404.html && cd .. && gh-pages -d dist"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.2.1",
    "bulma": "^0.9.4",
    "firebase": "^9.15.0",
    "vue": "^3.2.45",
    "vue-router": "^4.1.6",
    "vuex": "^4.1.0",
    "vuex-persistedstate": "^4.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.0.0",
    "vite": "^4.0.0"
  }
}
